{
  "name": "build-project-with-meta",
  "version": "1.0.1",
  "description": "docker build + git",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bin": {
    "docker-build-with-meta": "build.sh"
  },
  "author": "Anatoliy",
  "license": "ISC",
  "dependencies": {
    "chalk-cli": "^3.0.0",
    "json": "^9.0.4"
  }
}
